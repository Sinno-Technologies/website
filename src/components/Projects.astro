---
import cardData from "../data/cardData";
import Card from "./Card.astro";
import parkland from "../assets/ParklandPM.png";
import unum from "../assets/unumHealth.png";
import login from "../assets/Login.png";
import ramp from "../assets/rampatl.png";

const imgArr = [parkland, unum, login, ramp];
---

<section class="mainContainer" id="projects">
    <h3 class="projects">Projects</h3>
    <ul class="cardContainer">
        {cardData.map((card, index) => (
            <Card
              key={index}
              title={card.title}
              body={card.body}
              img={imgArr[index]}
              href={card.href}
              alt={card.alt}
            />
          ))}
    </ul>
    <a href="https://github.com/jamilsinno?tab=repositories" target="_blank" rel="noreferrer noopener nofollow" class="buttonProjects">View more of my projects on GitHub</a>
</section>

<style>
    .mainContainer{
        width:80%;
        height: auto;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
        padding: 2rem 1rem;
        gap: 2rem;
    }
    
    .container {
        width: 100%;
        height: auto;
        padding: 1rem;
    }

    .projects{
        font-size: 3.5rem;
        font-weight: bold;
    }

    .cardContainer{
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 1rem;
    }

    .buttonProjects{
        padding: 1rem;
        font-size: 1.8rem;
        border: 2px solid var(--yellow); 
        border-radius: 1em;
    }

    .buttonProjects:hover {
        background-color: var(--yellow);
        color: var(--black);
        border: 2px solid var(--white);
        transition: 0.3s;
    }
    @media (max-width: 1080px) {
        .cardContainer{
            grid-template-columns: 1fr;
        }
    }
</style>
